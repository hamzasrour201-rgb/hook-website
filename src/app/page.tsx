// This file should not be reached — next-intl middleware handles locale routing
// from root '/' to '/en' (default locale strips prefix via 'as-needed')
export default function RootPage() {
  return null
}
